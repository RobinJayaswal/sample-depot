<% if @current_item == line_item %>
<li id="current_item">
<% else %>
<li>
<%end%>
<%= line_item.quantity %> &times; <%= line_item.product.title %> Price: <%=number_to_currency(line_item.line_price)%> 

<% if !@from_email%>
<%= button_to('Remove', removeItem_path(item: line_item), remote: true)%>
<% end %>
</li>

<% @from_email = false %>
